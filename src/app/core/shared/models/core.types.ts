export type pathImageSVG = `${string}.svg`; // an SVG file is expected

export type validEmail = `${string}@utm.edu.ec`; // correo institucional del administrador

export type UUID = `${string}-${string}-${string}-${string}-${string}`

export type placesID = `places_${number}`;
