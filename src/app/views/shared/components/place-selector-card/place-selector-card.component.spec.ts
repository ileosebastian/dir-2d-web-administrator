import { ComponentFixture, TestBed, waitForAsync } from '@angular/core/testing';
import { IonicModule } from '@ionic/angular';

import { PlaceSelectorCardComponent } from './place-selector-card.component';

describe('PlaceSelectorCardComponent', () => {
  // let component: PlaceSelectorCardComponent;
  // let fixture: ComponentFixture<PlaceSelectorCardComponent>;

  // beforeEach(waitForAsync(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ PlaceSelectorCardComponent ],
  //     imports: [IonicModule.forRoot()]
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(PlaceSelectorCardComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // }));

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
