import { ComponentFixture, TestBed, waitForAsync } from '@angular/core/testing';
import { IonicModule } from '@ionic/angular';

import { ProfessorSelectorCardComponent } from './professor-selector-card.component';

describe('ProfessorSelectorCardComponent', () => {
  // let component: ProfessorSelectorCardComponent;
  // let fixture: ComponentFixture<ProfessorSelectorCardComponent>;

  // beforeEach(waitForAsync(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ ProfessorSelectorCardComponent ],
  //     imports: [IonicModule.forRoot()]
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(ProfessorSelectorCardComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // }));

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
