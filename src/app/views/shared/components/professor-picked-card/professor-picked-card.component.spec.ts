import { ComponentFixture, TestBed, waitForAsync } from '@angular/core/testing';
import { IonicModule } from '@ionic/angular';

import { ProfessorPickedCardComponent } from './professor-picked-card.component';

describe('ProfessorPickedCardComponent', () => {
  // let component: ProfessorPickedCardComponent;
  // let fixture: ComponentFixture<ProfessorPickedCardComponent>;

  // beforeEach(waitForAsync(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ ProfessorPickedCardComponent ],
  //     imports: [IonicModule.forRoot()]
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(ProfessorPickedCardComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // }));

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
