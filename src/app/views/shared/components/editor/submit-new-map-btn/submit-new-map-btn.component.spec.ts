import { ComponentFixture, TestBed, waitForAsync } from '@angular/core/testing';
import { IonicModule } from '@ionic/angular';

import { SubmitNewMapBtnComponent } from './submit-new-map-btn.component';

describe('SubmitNewMapBtnComponent', () => {
  // let component: SubmitNewMapBtnComponent;
  // let fixture: ComponentFixture<SubmitNewMapBtnComponent>;

  // beforeEach(waitForAsync(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ SubmitNewMapBtnComponent ],
  //     imports: [IonicModule.forRoot()]
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(SubmitNewMapBtnComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // }));

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
