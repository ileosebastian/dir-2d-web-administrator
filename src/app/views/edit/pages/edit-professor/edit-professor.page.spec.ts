import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EditProfessorPage } from './edit-professor.page';

describe('EditProfessorPage', () => {
  // let component: EditProfessorPage;
  // let fixture: ComponentFixture<EditProfessorPage>;

  // beforeEach((() => {
  //   fixture = TestBed.createComponent(EditProfessorPage);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // }));

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
