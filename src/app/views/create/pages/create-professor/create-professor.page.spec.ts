import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CreateProfessorPage } from './create-professor.page';

describe('CreateProfessorPage', () => {
  // let component: CreateProfessorPage;
  // let fixture: ComponentFixture<CreateProfessorPage>;

  // beforeEach(async(() => {
  //   fixture = TestBed.createComponent(CreateProfessorPage);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // }));

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
