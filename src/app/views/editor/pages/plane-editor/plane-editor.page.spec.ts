import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PlaneEditorPage } from './plane-editor.page';

describe('PlaneEditorPage', () => {
  // let component: PlaneEditorPage;
  // let fixture: ComponentFixture<PlaneEditorPage>;

  // beforeEach(async(() => {
  //   fixture = TestBed.createComponent(PlaneEditorPage);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // }));

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
