import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SearchProfessorPage } from './search-professor.page';

describe('SearchProfessorPage', () => {
  // let component: SearchProfessorPage;
  // let fixture: ComponentFixture<SearchProfessorPage>;

  // beforeEach(async(() => {
  //   fixture = TestBed.createComponent(SearchProfessorPage);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // }));

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
